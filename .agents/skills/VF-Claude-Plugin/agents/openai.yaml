interface:
  display_name: "VF Claude Plugin"
  short_description: "Repo-specific patterns and workflows for VF-Claude-Plugin"
  default_prompt: "Use the VF-Claude-Plugin repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true